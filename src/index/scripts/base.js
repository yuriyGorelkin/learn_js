/*let x = +prompt ('Please enter the value X', '');
let n = +prompt ('Please enter the value N', '');

function pow(x, n) {
  let result = x;

  for (let i = 1; i < n; i++) {
    result *= x;
  }

  return result;
}

alert( pow(x, n) );*/

const button = document.querySelector('#btnToggleModal');
console.log (button);